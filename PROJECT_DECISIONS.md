# OmniPM 架构决策记录（ADR）

> 只追加（Append-only）。每条决策记录永不修改，仅追加。

---

## [决策] #001 — 纯 Markdown 格式，零依赖

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion
- **议题**：提示词格式选型
- **背景**：需要跨 4+ 模型家族兼容，需避免平台锁定
- **选项**：
  - A: XML 标签格式（平台特有语法，高结构化）
  - B: 纯 Markdown 格式（通用语法，跨平台兼容）
- **结论**：选择 B。Markdown 在所有目标平台均可解析，不依赖 Function Calling 或平台特有标签。牺牲部分结构化精度换取最大兼容面。
- **影响范围**：OMNIPM_SYSTEM_PROMPT.md 全文、所有模块文件
- **替代方案记录**：XML 标签在特定平台（如 Claude）的结构化程度更高，但会锁定平台。

---

## [决策] #002 — 双文件记忆架构

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion
- **议题**：项目状态与决策历史的存储分离
- **背景**：需要持久化项目状态（跨会话恢复）和决策历史（审计追溯），两种数据有不同的读写模式
- **选项**：
  - A: 单文件存储所有信息
  - B: PROJECT_MEMORY.md（覆盖写，状态追踪）+ PROJECT_DECISIONS.md（只追加，决策日志）
- **结论**：选择 B。状态追踪需要频繁覆盖写（当前步骤、检查点），决策日志需要不可变追加（架构决策、评审结论）。混在一起会导致并发写入冲突和审计困难。
- **影响范围**：PROJECT_MEMORY.md、PROJECT_DECISIONS.md、§七 项目记忆机制
- **替代方案记录**：单文件方案更简单，但覆盖写会丢失历史决策；追加写会使状态追踪变得复杂。

---

## [决策] #003 — 三级门控机制

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion
- **议题**：用户确认节点的粒度设计
- **背景**：Agent 需要在"自动推进效率"与"用户控制权"之间取得平衡
- **选项**：
  - A: 每个子步骤都等待用户确认（最大控制权，低效率）
  - B: 仅在 3 个关键节点暂停（GATE-REQUIREMENT / GATE-DESIGN / GATE-ACCEPTANCE）
  - C: 完全自动推进，仅在最终交付时确认（最高效率，低控制权）
- **结论**：选择 B。三个 GATE 节点对应项目的三个不可逆决策点（需求基线、设计冻结、交付验收），在效率和可控性之间取得最佳平衡。
- **影响范围**：§六 门控协议、状态转换矩阵
- **替代方案记录**：选项 A 适合高风险合规项目（待 v1.0.0 作为可选的"严格模式"）。

---

## [决策] #004 — 混合型"交织矩阵"架构

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion（综合 8 位专家评审意见）
- **议题**：混合型项目如何从"并行"升级为"深度融合"
- **背景**：v0.2.1 的混合型处理仅是"主导类型+补充模块"的并行注入，两种类型的工作流实际独立运行。v0.3.0 需要实现真正的协同。
- **选项**：
  - A: 为每种混合对定义完整的新工作流（2^5=32 种组合，维护成本不可接受）
  - B: 交织矩阵模型——保留现有 5 种工作流，通过交织点定义 Step A~E 的交叉引用规则
  - C: 让 Agent 在运行时动态判断交叉点（依赖模型能力，不可靠）
- **结论**：选择 B。交织矩阵是"配置优于代码"的思路：每种混合对仅定义交叉点（~80行），而非重复整个工作流（~500行）。4 对高频组合完整实现，6 对骨架定义推迟至 v1.0.0。
- **影响范围**：modules/weaving/（10个文件）、OMNIPM_SYSTEM_PROMPT.md §3.3.1、modules/router_logic.md §5
- **替代方案记录**：选项 A 最精确但维护成本过高。选项 C 依赖模型推理能力，在 GPT-4o/Gemini 上不可靠。

---

## [决策] #005 — 跨模型兼容性以"适配配置层"而非"多版本提示词"实现

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion（综合 8 位专家评审意见）
- **议题**：如何保证 4 种模型上的行为一致性
- **背景**：不同模型对同一提示词的遵循程度不同，需要差异化调优。维护 4 份独立提示词会导致变更同步困难。
- **选项**：
  - A: 维护 4 份独立提示词（每个模型一份）
  - B: 单一主提示词 + 轻量级模型适配配置层（<200 tokens）
  - C: 不做任何适配，依赖模型自身能力
- **结论**：选择 B。适配配置层仅包含 5 个参数和一个已知行为特征列表，放在提示词固定段末尾（不破坏前缀缓存）。所有模型的提示词主体完全一致，变更只需改一处。
- **影响范围**：MODEL_PROFILES.md、OMNIPM_SYSTEM_PROMPT.md §十四附
- **替代方案记录**：选项 A 可做到每个模型的最优体验，但 4 份文件的同步维护成本会随功能增长而指数上升。

---

## [决策] #006 — BDS ≤ 10 作为跨模型兼容性目标

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion（综合测试架构师和DevOps工程师意见）
- **议题**：跨模型行为一致性的可接受偏差阈值
- **背景**：需要在"追求 100% 一致性（不现实）"和"放任模型间差异（影响用户体验）"之间找到平衡点
- **选项**：
  - A: BDS ≤ 5（严格，可能需要大量模型特定适配）
  - B: BDS ≤ 10（≥90% 一致性，务实目标）
  - C: BDS ≤ 20（宽松，仅保证核心路径一致）
- **结论**：选择 B。BDS ≤ 10 意味着核心路径（状态机、GATE、安全）几乎完全一致，仅在输出格式细节上有小幅偏差。24 个用例 × 1.0~1.5 权重，目标确保安全相关项零偏差，非关键输出格式允许少量偏差。
- **影响范围**：CROSS_MODEL_VALIDATION_PROTOCOL.md、CROSS_MODEL_TEST_SUITE.md
- **替代方案记录**：选项 A 需要大量模型特定适配代码，推迟至 v1.0.0。选项 C 过于宽松，可能掩盖重要的行为偏差。

---

## [决策] #007 — PI Agent 原生适配决策

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion（综合 8 位专家评审意见）
- **议题**：OmniPM 从跨模型通用提示词转型为 PI Agent 原生运行时
- **背景**：v0.3.0 完成了跨模型兼容性验证，但"通用"意味着"均不深入"。PI Agent 的工具调用循环、AGENTS.md 原生支持、自扩展能力使其成为 OmniPM 最理想的运行环境。
- **选项**：
  - A: 保持跨模型通用 + 继续维护 4 套模型适配参数
  - B: 完全转向 PI Agent，移除跨模型兼容层
  - C: 分叉为两个版本（通用版 + PI 版）
- **结论**：选择 B。移除第十四附（~60行）、MODEL_PROFILES.md、CROSS_MODEL_TEST_SUITE.md、CROSS_MODEL_VALIDATION_PROTOCOL.md 及所有跨模型适配代码。OmniPM 完整保留全部核心能力。
- **影响范围**：OMNIPM_SYSTEM_PROMPT.md、modules/security_gate.md、AGENTS.md
- **替代方案记录**：A 被淘汰因为维护成本高于收益；C 被淘汰因为分叉维护负担过重。

---

## [决策] #008 — 能力自发现层（CDL）架构决策

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion（综合 8 位专家评审意见，Step B 决议）
- **议题**：CDL 的搜索范围、质量门禁和安装策略
- **背景**：v0.3.0 的模块加载是静态的。用户需要手动发现和配置外部能力。v1.0.0-PI 需要实现主动能力发现。
- **选项**：
  - A: 仅搜索 PI 生态（Skills Registry / npm / MCP Registry）
  - B: PI + GitHub 双生态搜索（含 Actions、Dev Containers、模板仓库、参考实现等）
  - C: 开放式搜索（PI + GitHub + GitLab + 任意 URL）
- **结论**：选择 B。PI 生态覆盖核心能力，GitHub 生态覆盖辅助能力。C 的部分功能通过 .pi/config.yaml 的 custom_sources 字段实现（含私有 GitLab），任意 URL 因安全风险暂不支持。
- **影响范围**：OMNIPM_SYSTEM_PROMPT.md 第十六节、modules/cdl_quality_gate.md、modules/cdl_guide.md、.pi/ 目录
- **替代方案记录**：A 被淘汰因为范围过窄；C 部分采纳——通过 custom_sources 支持用户自定义扩展源。

---

## [决策] #011 — v2.0.0 自编排工作流引擎重构

- **时间戳**：2026-07-21T13:00:00Z
- **决策人**：Orion（综合系统架构师/需求分析师/测试架构师/安全专家/DevOps 评审意见）
- **议题**：从静态流水线（v1.0.0-PI）到自编排工作流引擎（v2.0.0）的架构级重构
- **背景**：用户指出 v1.0.0-PI 的本质是"静态脚本"——固定 5 步管道、固定 8 专家评审团、固定 7 维度设计。这对不同项目"一种药治百病"，前端专家评审纯后端 API，SEO 专家评审 CLI 工具——毫无意义。需要让 Orion 自己根据项目动态设计工作流。
- **选项**：
  - A: 在 v1.0.0-PI 上打补丁（增加"跳过不需要的专家"的条件判断）
  - B: 架构级重构——Meta-Orion + Execution-Orion 双层模型，用 DAG 替代管道
- **结论**：选择 B。补丁式改进治标不治本——核心问题是"谁来设计工作流"。v2.0.0 把工作流设计权交还给 Orion 本身：
  - **Meta-Orion**（元层）：项目启动时深度分析 → 生成 DAG → 按需组装专家 → 输出执行计划
  - **Execution-Orion**（执行层）：按 DAG 执行 → 闭环监控 → 根因分析 → 自动修正
  - **核心变化**：
    - 固定 5 步管道 → 动态 DAG（3-15 节点）
    - 固定 8 人评审 → 按需激活专家（0-13 人，4 种强度）
    - 关键词路由 → 深度语义分析（领域/风险/域/复杂度 5 维度）
    - 固定回退表 → 根因分析+动态回退
    - 固定 3 GATE → 1-5 GATE 风险自适应
    - 新增熔断机制（同节点最多修正 3 次）
    - 新增 META-GATE（分析结论用户确认后才生成 DAG）
  - **保留**：安全协议、输出格式、记忆机制、CDL、确认信号字典
  - **废弃**：router_logic.md（静态路由）、4 种差异化工作流（DAG 替代）、10 对交织矩阵（跨域分析替代）
- **影响范围**：OMNIPM_SYSTEM_PROMPT.md（完全重写）、modules/meta_analyzer.md（新增）、modules/dynamic_orchestrator.md（新增）、modules/roles.md（更新）、modules/router_logic.md（废弃）、AGENTS.md（更新）
- **替代方案记录**：选项 A 被淘汰因为它无法解决根本问题——"谁决定工作流"。增加条件判断只是让静态脚本更复杂，而不是真正动态。

---

## [决策] #010 — CDL 能力层安装完成

- **时间戳**：2026-07-21T12:27:00Z
- **决策人**：Orion（项目总负责人）
- **议题**：OmniPM v1.0.0-PI CDL 能力自发现层实际部署
- **背景**：CDL 层在 Step C 设计并生成了推荐清单（.pi/skills.yaml、.pi/mcp.yaml、.pi/subagents.yaml），但部分 MCP 服务的 npm 全局安装尚未执行。用户要求"为项目安装能力层"以完成 CDL 从纸面到运行时的完整闭环。
- **选项**：
  - A: 仅保留 YAML 声明（CDL 纸面完成）
  - B: 逐一验证并安装全部缺失的 MCP 服务
- **结论**：选择 B。执行了以下安装动作：
  - **Skills**：全部 18 个 Skill 已在 `~/.agents/skills/` 就绪（vercel、supabase 于同日完成安装）
  - **MCPs**：安装 `@supabase/mcp-server-supabase@0.9.0`（数据库/后端）和 `@upstash/context7-mcp@3.2.4`（文档查询，注意包名已从 @anthropic/mcp-server-context7 迁移至 @upstash/context7-mcp）
  - **Subagents**：4 个内置 Subagent（Plan / code-review / Explore / general-purpose）由 PI Agent 原生提供
  - 更新 `.pi/mcp.yaml` 反映真实安装状态和正确包名
- **影响范围**：`.pi/mcp.yaml`、`PROJECT_MEMORY.md`、全局 npm 包列表
- **替代方案记录**：选项 A 被淘汰因为 CDL 的设计意图是"安装就绪"而非"纸面推荐"。

---

## [决策] #009 — v1.0.0-PI 项目验收通过

- **时间戳**：2026-07-21T00:00:00Z
- **决策人**：Orion（项目总负责人）
- **议题**：OmniPM v1.0.0-PI 项目交付验收
- **背景**：Step A-E 全部完成，GATE-ACCEPTANCE 用户确认通过。v1.0.0-PI 实现了从跨模型通用提示词到 PI Agent 原生运行时的战略转型，新增 CDL 能力自发现层。
- **选项**：
  - A: 验收通过，项目完成
  - B: 进入 v1.0.0 后续迭代
- **结论**：选择 A。全部 5 个阶段完整闭环，9 项质量门禁通过，v0.3.0 跨模型残留清零。已知 5 项限制记录在案，推迟至未来版本。
- **影响范围**：PROJECT_MEMORY.md（status: completed）、CHANGELOG.md
- **替代方案记录**：后续迭代方向包括：6 对骨架交织矩阵升级、LLM-as-Judge 自动化测试、跨会话自动恢复。

---
